// coervion means converting one type to another
console.log(Number(false));
console.log(Number(true));

/**
 * here associativity is left to right
 * false < 1 , becomes 0 < 1 which is true
 */
console.log(3<2<1);

/**
 * here fase > 3, 0 > 3, false
 */
console.log(1>2>3);
