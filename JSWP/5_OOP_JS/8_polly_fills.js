/**
 * A polly fills is a code or logic which add a functionality which lacks in a JS engine.
 */

 if(!Object.create) {
     /**
      * This code will execute only if the object.create function is not avaliable in the engine..
      */
 }